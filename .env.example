# Frame — Development Environment Variables
# Copy to .env and adjust as needed

# PostgreSQL connection (matches docker/docker-compose.yml defaults)
DATABASE_URL=postgresql://frame:frame@localhost:54320/frame
